# ── Clés API pour les LLM ──────────────────────────────────────
MISTRAL_API_KEY=

# ── Clés API pour les embeddings (optionnel) ──────────────────
# OpenAI embeddings (si vous ne voulez pas utiliser Transformers.js local)
OPENAI_API_KEY=

DATABASE_URL=

# ── Cache Redis (optionnel) ────────────────────────────────────
# URL de connexion Redis pour le cache des questions/réponses
# Format: redis://[:password@]host[:port][/db-number]
# Exemples:
#   - Local: redis://localhost:6379
#   - Avec auth: redis://:mypassword@localhost:6379
#   - Redis Cloud: redis://username:password@redis-12345.cloud.redislabs.com:12345
# Si non défini ou REDIS_ENABLED=false, l'application fonctionne sans cache
REDIS_URL=redis://localhost:6379
# Désactiver explicitement le cache même si REDIS_URL est défini
# REDIS_ENABLED=false

# ── Protection anti-spam ───────────────────────────────────────
# Liste d'IPs exemptées du rate limiting (séparées par des virgules)
# Utile pour les environnements de développement, tests, ou IPs de confiance
# Exemple: RATE_LIMIT_WHITELIST=127.0.0.1,::1,192.168.1.100
# RATE_LIMIT_WHITELIST=

# ── Administration ─────────────────────────────────────────────
# Mot de passe pour accéder à la page d'administration (/admin)
ADMIN_PASSWORD=

# ── Stockage des fichiers ──────────────────────────────────────
# Répertoire pour stocker les fichiers uploadés (PDF, TXT)
# Par défaut: uploads/ à la racine du projet
# UPLOADS_DIR=/var/data/ask-rules/uploads

# ── Réseau / TLS ───────────────────────────────────────────────
# À activer si vous êtes derrière un proxy SSL (inspection TLS d'entreprise)
# qui empêche le téléchargement des modèles HuggingFace.
# ⚠ Désactive la vérification des certificats TLS pour tout le processus Node.js.
# Ne pas utiliser en production sans comprendre les implications.
# NODE_TLS_REJECT_UNAUTHORIZED=0